# Django secret key — use a long random string in production.
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=change-me-in-production

# Set to True for local development only. Never enable in production.
DJANGO_DEBUG=True

# Comma-separated list of allowed hostnames (e.g. yourdomain.com,www.yourdomain.com).
# Defaults to localhost,127.0.0.1 if not set.
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Comma-separated trusted origins for CSRF protection.
# Required in production when running behind a reverse proxy.
# Example: https://yourdomain.com
DJANGO_CSRF_TRUSTED_ORIGINS=

# PostgreSQL connection URL.
DATABASE_URL=postgres://padd:padd@localhost:5432/padd

# Comma-separated list of user profile URLs granted admin access.
# Example: https://yourdomain.com/
PADD_ADMIN_URLS=
